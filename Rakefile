require_relative 'config/environment'
require 'sinatra/activerecord/rake'

desc 'starts a console'
task :console do
  Pry.start

  # tacos = FoodTruck.create(name: "Taco Truck", food_type: "tacos")
  # ice_cream = FoodTruck.create(name: "Sweets", food_type: "ice cream")

  # chels = Customer.create(name: "Chelsea")
  # zoe = Customer.create(name: "Zoe")

  # order1 = Order.create(customer_id: chels.id, food_truck_id: tacos.id)
  # order2 = Order.create(customer_id: zoe.id, food_truck_id: ice_cream.id)

end
